schema_version: "1.0"
name: 05-conjurer-webhook-double-fire
runs: 2
max_turns: 20
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit, Bash]
model: sonnet
plugins: ["../.."]
context:
  scaffold_script: scaffold.sh
execution:
  prompt: |
    Payment webhooks double-fire and two customers got charged twice.
    The handler is webhooks/payments.py.
